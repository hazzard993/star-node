{
  "name": "lua-star-node",
  "version": "0.1.1",
  "license": "MIT",
  "scripts": {
    "build": "tstl -p tsconfig.json && copyfiles -f node_modules/path/*.lua node_modules/fs/*.lua run"
  },
  "files": [
    "run/**",
    "run.lua"
  ],
  "devDependencies": {
    "copyfiles": "^2.3.0",
    "fs": "luapower/fs",
    "path": "luapower/path",
    "typescript-to-lua": "^0.35.0"
  },
  "resolutions": {
    "typescript": "4.0.3"
  }
}
